<p class="container">Hier komt informatie met de feitelijke gegevens over de stad.  Deze staat buiten de div zodat we de gehele schermbreedte nog kunnen gebruiken indien nodig.</p>
<div class="row">

  <div class="col s3 selections">
    left
  </div>

  <div class="col s9 topics">
    {{#liquid-bind topics as |topicsVersion|}}
      {{#each topicsVersion key="id" as |topic|}}
        <div class="topic" topic-id={{topic.title}} {{action "clickTopic" topic}}>
          topics here!
        </div>
      {{/each}}
    {{/liquid-bind}}
  </div>

</div>

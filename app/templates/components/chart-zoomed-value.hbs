<div class="controls">
  <button class="btn" {{action 'makePie'}}>pie</button>
  <button class="btn" {{action 'makeBar'}}>bar</button>
</div>

{{default-chart regionTheme=regionTheme chartType=chartType}}

<div class="widgets">
  <section class="widget">
    <header>
      <h3>More</h3>
    </header>
    <div class="tags">
      {{#each additionalTopics as |topic|}}
        <span>{{topic.title}}</span>
      {{/each}}
    </div>
  </section>
</div>
